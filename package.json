{
  "name": "todo_app_node_post",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=serve && (cd server && npm install) || (cd client && npm install)",
    "server": "node ./server/server.js",
    "client": "cd client && npm start"
  },
  "license": "ISC",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
